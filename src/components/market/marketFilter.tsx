// import { Form } from 'react-bootstrap';

const MarketFilter = () => {
    return (
        <>
            {/* <div className="box-filter">
                <div className="box-filter-title">Ways to Buy</div>
                <Form.Check type="radio" aria-label="radio 1" name="wayToBuy" label="All" defaultChecked={true} />
                {['Buy Now', 'Bid', 'Make Offer'].map((type) => (
                    <Form.Check type="radio" aria-label="radio 1" name="wayToBuy" key={type} label={type} />
                ))}
            </div>

            <div className="box-filter">
                <div className="box-filter-title">Price</div>
                <Form.Check type="radio" aria-label="radio 1" name="price" label="All" defaultChecked={true} />
                {['100+ BNB', '50-100 BNB', '10-50 BNB', '0-10 BNB'].map((type) => (
                    <Form.Check type="radio" aria-label="radio 1" name="price" key={type} label={type} />
                ))}
            </div>

            <div className="box-filter">
                <div className="box-filter-title">DEGO Par Value</div>
                <Form.Check type="radio" aria-label="radio 1" name="dego" label="All" defaultChecked={true} />
                {['Below 10 DEGO', '10-50 DEGO', '50-100 DEGO', '100-200 DEGO', 'Above 200 DEGO'].map((type) => (
                    <Form.Check type="radio" aria-label="radio 1" name="dego" key={type} label={type} />
                ))}
            </div>

            <div className="box-filter">
                <div className="box-filter-title">Level</div>
                <Form.Check type="radio" aria-label="radio 1" name="level" label="All" defaultChecked={true} />
                {[...Array(6).keys()].map((type) => (
                    <Form.Check type="radio" aria-label="radio 1" name="level" key={type} label={`Level ${type}`} />
                ))}
            </div> */}
        </>
    )
}

export default MarketFilter